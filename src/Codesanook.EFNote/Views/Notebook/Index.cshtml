@model IEnumerable<Notebook>
@{
  ViewData["Title"] = "Notebook - index";
}

<div class="secondary-menu">
  <div class="col-12">
    <a class="btn btn-light" href="@Url.Action("Create")" role="button">
      <i class="fa fa-plus-circle"></i> New
    </a>
    <a class="btn btn-light" href="@Url.Action("ChangedLog")" role="button">
      <i class="fa fa-clock"></i> Changed log
    </a>
  </div>
</div>

<div class="main-panel">
  <div class="col-12">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Id</th>
          <th>Name</th>
          <th># of notes</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        @foreach (var notebook in Model)
        {
          <tr>
            <td>@notebook.Id</td>
            <td>@notebook.Name</td>
            <td>@notebook.Notes.Count()</td>
            <td>
              <a class="btn btn-light" href="@Url.Action(
                  "Edit",
                  "Notebook", new { id = notebook.Id}
                )" role="button">
                <i class="fa fa-edit"></i> Edit
              </a>
              <a class="btn btn-light" href="@Url.Action(
                  "Delete",
                  "Notebook", new { id = notebook.Id}
                  )" role="button">
                <i class="fa fa-trash"></i> Delete
              </a>
            </td>
          </tr>
        }
      </tbody>
    </table>

  </div>

</div>
