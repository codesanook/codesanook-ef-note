@model IEnumerable<NotebookIndexViewModel>
@{
  ViewData["Title"] = "Notebook - index";
}

<div class="secondary-menu">
  <div class="col-12">
    <a class="btn btn-light" href="@Url.Action("Create")" role="button">
      <i class="fa fa-plus-circle"></i> New
    </a>
  </div>
</div>

<div class="main-panel">
  <div class="col-12">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Id</th>
          <th>Name</th>
          <th>Valid from</th>
          <th>Valid to</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        @foreach (var item in Model)
        {
          <tr>
            <td>@item.Notebook.Id</td>
            <td>@item.Notebook.Name</td>
            <td>@item.ValidFrom</td>
            <td>@item.ValidTo</td>
            <td>
              <a class="btn btn-light" href="@Url.Action(
                  "Edit",
                  "Notebook", new { id = item.Notebook.Id}
                )" role="button">
                <i class="fa fa-edit"></i> Edit
              </a>
              <a class="btn btn-light" href="@Url.Action(
                  "Delete",
                  "Notebook", new { id = item.Notebook.Id}
                  )" role="button">
                <i class="fa fa-trash"></i> Delete
              </a>
            </td>
          </tr>
        }
      </tbody>
    </table>

  </div>

</div>
